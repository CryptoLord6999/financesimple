# Review package: 5205d40..HEAD

## Commits
3e3e777 feat: show current report period and its length on the Отчёт page
d98b20f feat: allow undoing the most recent report submission
f30a78e feat: badge and date-range display for submitted vs fixed reports
e8c677d feat: doSubmitReport closes the period, doJustFixReport snapshots without resetting
20b4e9b feat: add Сдать отчёт / Просто зафиксировать choice sheet
b0be50e fix: setPeriod refreshes the Детали top summary, not just the tree below
5086efa feat: calcAll accepts a period; Детали top summary follows the date filter
bf51a16 feat: filterTx treats no-period calls as since-last-report, not lifetime
a7d897b feat: persist reportPeriodStart alongside capital/savedReports
72aea8a fix: scrub all occurrences of the real Firebase project id from the test copy
6ad18c6 chore: add safe test-copy script for Firebase-free browser verification
501485d chore: ignore local sdd scratch state

## Files changed
 .gitignore             |  2 ++
 index.html             | 50 ++++++++++++++++++++++++++++++++++----------------
 test/make-test-copy.sh | 12 ++++++++++++
 3 files changed, 48 insertions(+), 16 deletions(-)

## Diff
diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..f4b9f52
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,2 @@
+.superpowers/
+index.test.html
diff --git a/index.html b/index.html
index 1f607bd..8be5013 100644
--- a/index.html
+++ b/index.html
@@ -507,29 +507,30 @@ body.light-theme .toast { background: rgba(255,255,255,0.95); border: 1px solid
 <div class="summary-item"><div class="summary-label"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#007AFF" stroke-width="2.5" style="vertical-align:-2px;"><path d="M3 17L9 11L13 15L21 7"/></svg> Оборот</div><div class="summary-val text-blue" id="sumTurnover">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#FF9500" stroke-width="2" style="vertical-align:-2px;"><circle cx="12" cy="12" r="9"/><line x1="12" y1="6" x2="12" y2="18"/></svg> Выручка</div><div class="summary-val text-orange" id="sumRevenue">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="11" height="11" viewBox="0 0 24 24" style="vertical-align:-1px;"><circle cx="12" cy="12" r="9" fill="#34C759"/></svg> Доходы</div><div class="summary-val text-green" id="sumIncome">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="11" height="11" viewBox="0 0 24 24" style="vertical-align:-1px;"><circle cx="12" cy="12" r="9" fill="#FF3B30"/></svg> Расходы</div><div class="summary-val text-red" id="sumExpense">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#AF52DE" stroke-width="2" style="vertical-align:-2px;"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 0 1-8 0"/></svg> Траты</div><div class="summary-val text-purple" id="sumSpend">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="11" height="11" viewBox="0 0 24 24" style="vertical-align:-1px;"><circle cx="12" cy="12" r="9" fill="#AEAEB2"/></svg> Баланс</div><div class="summary-val" id="sumBalance">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="12" height="12" viewBox="0 0 24 24" fill="#AF52DE" style="vertical-align:-2px;"><path d="M12 21C9 18.9 3 14.6 3 9.5 3 6.5 5.3 4 8.3 4c1.7 0 3 .8 3.7 2 .7-1.2 2-2 3.7-2 3 0 5.3 2.5 5.3 5.5 0 5.1-6 9.4-9 11.5z"/></svg> Капитал</div><div class="summary-val text-blue" id="sumCapital">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="11" height="11" viewBox="0 0 24 24" style="vertical-align:-1px;"><circle cx="12" cy="12" r="9" fill="#5AC8FA"/></svg> На сайте</div><div class="summary-val text-orange" id="sumSite">0 ₽</div></div>
 <div class="summary-item"><div class="summary-label"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#5AC8FA" stroke-width="2" style="vertical-align:-2px;"><polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/></svg> Маржа</div><div class="summary-val text-teal" id="sumMargin">0 ₽</div></div>
 </div></div>
+<div id="currentPeriodLabel" style="text-align:center;font-size:0.75rem;color:var(--ios-text-secondary);margin:-4px 0 14px;"></div>
 <div class="report-actions-group">
 <button class="btn btn-secondary" onclick="openCapitalAdjustment()" style="border-color:rgba(175,82,222,0.3);color:var(--ios-purple);">
 <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
 Корректировка капитала
 </button>
-<button class="btn btn-primary" id="fixReportBtn" onclick="doFixReport()"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L12 14"/><path d="M9 5L12 2L15 5"/><circle cx="12" cy="18" r="2" fill="currentColor"/></svg>Зафиксировать</button>
+<button class="btn btn-primary" id="fixReportBtn" onclick="openReportChoice()"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L12 14"/><path d="M9 5L12 2L15 5"/><circle cx="12" cy="18" r="2" fill="currentColor"/></svg>Зафиксировать</button>
 <button class="btn btn-secondary" onclick="doShareCurrentReport()"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>Поделиться</button>
 </div>
-<div id="lastFixedBadge" style="display:none;background:rgba(52,199,89,0.12);border-radius:12px;padding:12px;margin-bottom:16px;font-size:0.8rem;color:var(--ios-green);font-weight:500;align-items:center;gap:6px;"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>Последний: <span id="lastFixedDate"></span></div>
+<div id="lastFixedBadge" style="display:none;background:rgba(52,199,89,0.12);border-radius:12px;padding:12px;margin-bottom:16px;font-size:0.8rem;color:var(--ios-green);font-weight:500;align-items:center;gap:6px;justify-content:space-between;"><div style="display:flex;align-items:center;gap:6px;"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>Последний: <span id="lastFixedDate"></span></div><button id="lastFixedUndo" onclick="undoLastSubmit()" style="display:none;background:none;border:none;color:var(--ios-green);font-weight:600;font-size:0.8rem;text-decoration:underline;cursor:pointer;padding:0;">Отменить</button></div>
 <div class="section-title">Зафиксированные отчёты</div>
 <div id="reportsList"></div>
 </div></div>
 <div class="page" id="page-goal"><div class="page-content">
 <div style="text-align:center;margin-bottom:20px;"><div class="section-title" style="font-size:1.1rem;margin-bottom:8px;justify-content:center;"><svg width="24" height="24" viewBox="0 0 24 24" fill="none" style="vertical-align:middle;margin-right:8px;"><circle cx="12" cy="12" r="10" stroke="var(--ios-orange)" stroke-width="2"/><circle cx="12" cy="12" r="6" stroke="var(--ios-orange)" stroke-width="2"/><circle cx="12" cy="12" r="2" fill="var(--ios-orange)"/></svg>Цель по выручке</div></div>
 <div class="goal-ring-wrapper"><svg viewBox="0 0 200 200"><circle class="goal-ring-bg" cx="100" cy="100" r="85"/><circle class="goal-ring-fill" id="goalRingFill" cx="100" cy="100" r="85" pathLength="1"/></svg><div class="goal-ring-center"><div><span class="goal-percent" id="goalPercent">0</span><span class="goal-percent-sign">%</span></div><div class="goal-subtitle" id="goalSubtitle">Цель не задана</div></div></div>
 <div class="goal-stats">
 <div class="goal-stat-card"><div class="goal-stat-label">Выручка</div><div class="goal-stat-value text-orange" id="goalCurrentRevenue">0 ₽</div></div>
 <div class="goal-stat-card"><div class="goal-stat-label">Цель</div><div class="goal-stat-value text-blue" id="goalTargetEl">—</div></div>
 <div class="goal-stat-card"><div class="goal-stat-label">Осталось</div><div class="goal-stat-value text-red" id="goalRemaining">—</div></div>
@@ -703,20 +704,29 @@ body.light-theme .toast { background: rgba(255,255,255,0.95); border: 1px solid
 <input type="number" id="capitalAdjAmount" class="setting-input" placeholder="Например: 50000" step="0.01" min="0">
 <div class="setting-hint">Капитал будет установлен на введённое значение</div>
 </div>
 <div class="setting-group">
 <div class="setting-label">Комментарий (необязательно)</div>
 <input type="text" id="capitalAdjComment" class="setting-input" placeholder="Например: Внесение наличных" style="text-align:left;font-size:0.9rem;">
 </div>
 <button class="btn btn-primary" onclick="saveCapitalAdjustment()" style="margin-top:8px;background:var(--ios-purple);">Установить</button>
 <button class="btn btn-secondary" onclick="closeCapitalAdjustment()" style="margin-top:8px;">Отмена</button>
 </div>
+<div class="sheet-overlay" id="reportChoiceOverlay" onclick="closeReportChoice()"></div>
+<div class="bottom-sheet" id="reportChoiceSheet">
+<div class="sheet-handle"></div>
+<div class="sheet-title">Зафиксировать показатели</div>
+<div class="setting-hint" style="text-align:center;margin-bottom:12px;">«Сдать отчёт» сбросит на экране оборот, доходы, расходы, выручку, траты и маржу до 0 — начнётся новый отчётный период. Балансы счетов и капитал не изменятся. Полная история останется доступна в «Деталях».</div>
+<button class="btn btn-primary" onclick="doSubmitReport()" style="margin-top:8px;">Сдать отчёт</button>
+<button class="btn btn-secondary" onclick="doJustFixReport()" style="margin-top:8px;">Просто зафиксировать</button>
+<button class="btn btn-secondary" onclick="closeReportChoice()" style="margin-top:8px;">Отмена</button>
+</div>
 <div class="sheet-overlay" id="permOverlay" onclick="closePermSheet()"></div>
 <div class="bottom-sheet" id="permSheet">
 <div class="sheet-handle"></div>
 <div class="sheet-title"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>Права доступа</div>
 <div id="permMemberInfo" style="background:var(--ios-card);border-radius:var(--radius-ios-sm);padding:14px;margin-bottom:16px;border:1px solid var(--ios-separator);display:flex;align-items:center;gap:12px;"></div>
 <div class="setting-group" style="margin-bottom:12px;">
 <div class="setting-label">Имя партнёра</div>
 <input type="text" id="permMemberName" class="setting-input" placeholder="Введите имя" style="text-align:left;font-size:0.95rem;">
 </div>
 <div style="font-size:0.75rem;font-weight:600;color:var(--ios-text-secondary);text-transform:uppercase;letter-spacing:0.05em;margin-bottom:10px;">Выберите роль:</div>
@@ -1155,47 +1165,47 @@ const viewingOwner = getViewingOwner();
 userRef = firebase.database().ref(`users/${viewingOwner}`);
 }
 function subscribeToData() {
 if (!useFirebase || !userRef) return;
 if (dataListener) { userRef.off('value', dataListener); dataListener = null; }
 showSkeletons(); updateSync('syncing');
 dataListener = userRef.on('value', snap => {
 const d = snap.val();
 if (d?.holders) {
 holders = d.holders; activeHolderId = d.activeHolderId || Object.keys(holders)[0]; activeAccountId = d.activeAccountId;
-capital = d.capital || 0; savedReports = d.savedReports || {}; incomePercent = d.incomePercent || 7; expensePercent = d.expensePercent || 2.5;
+capital = d.capital || 0; savedReports = d.savedReports || {}; reportPeriodStart = d.reportPeriodStart || null; incomePercent = d.incomePercent || 7; expensePercent = d.expensePercent || 2.5;
 goalTarget = d.goalTarget || 0; goalHistory = d.goalHistory || [];
 const hh = holders[activeHolderId];
 if (hh && (!activeAccountId || !hh.accounts[activeAccountId])) activeAccountId = Object.keys(hh.accounts || {})[0] || null;
 renderAll(); updateSync('realtime');
 } else { loadLocal(); renderAll(); saveServer(); }
 }, err => { loadLocal(); renderAll(); updateSync('offline'); });
 }
-let holders = {}, activeHolderId = null, activeAccountId = null, capital = 0, savedReports = {};
+let holders = {}, activeHolderId = null, activeAccountId = null, capital = 0, savedReports = {}, reportPeriodStart = null;
 let incomePercent = 7, expensePercent = 2.5, detailPeriod = 'today', detailCustomRange = null;
 let isOnline = navigator.onLine, isEditMode = false, expandedHolders = {}, expandedDetailAccounts = {};
 let goalTarget = 0, goalHistory = [];
-const saveLocal = () => { try { localStorage.setItem('fl_holders', JSON.stringify(holders)); localStorage.setItem('fl_ah', activeHolderId); localStorage.setItem('fl_aa', activeAccountId); localStorage.setItem('fl_cap', capital.toString()); localStorage.setItem('fl_reports', JSON.stringify(savedReports)); localStorage.setItem('fl_ip', incomePercent.toString()); localStorage.setItem('fl_ep', expensePercent.toString()); localStorage.setItem('fl_goal', goalTarget.toString()); localStorage.setItem('fl_goal_history', JSON.stringify(goalHistory)); } catch(e) {} };
-const loadLocal = () => { try { const h = localStorage.getItem('fl_holders'); if (h) { holders = JSON.parse(h); activeHolderId = localStorage.getItem('fl_ah'); activeAccountId = localStorage.getItem('fl_aa'); if (!activeHolderId || !holders[activeHolderId]) activeHolderId = Object.keys(holders)[0]; const hh = holders[activeHolderId]; if (hh && (!activeAccountId || !hh.accounts[activeAccountId])) activeAccountId = Object.keys(hh.accounts || {})[0] || null; } else { const hid = 'h_' + Date.now(), aid = 'a_' + Date.now(); holders = { [hid]: { id: hid, name: 'Основной', accounts: { [aid]: { id: aid, name: 'Основной', balance: 0, transactions: [], createdAt: new Date().toISOString() } }, createdAt: new Date().toISOString() } }; activeHolderId = hid; activeAccountId = aid; } savedReports = JSON.parse(localStorage.getItem('fl_reports') || '{}'); incomePercent = parseFloat(localStorage.getItem('fl_ip')) || 7; expensePercent = parseFloat(localStorage.getItem('fl_ep')) || 2.5; capital = parseFloat(localStorage.getItem('fl_cap')) || 0; goalTarget = parseFloat(localStorage.getItem('fl_goal')) || 0; goalHistory = JSON.parse(localStorage.getItem('fl_goal_history') || '[]'); } catch(e) {} };
-const saveServer = () => { if (!useFirebase || !isOnline) { updateSync('local'); return; } if (saveTimeout) clearTimeout(saveTimeout); saveTimeout = setTimeout(async () => { if (isSyncing) return; isSyncing = true; updateSync('syncing'); try { const s = calcAll(); const viewingOwner = getViewingOwner(); await firebase.database().ref(`users/${viewingOwner}`).update({ holders, activeHolderId, activeAccountId, capital, savedReports, totalIncome: s.totalIncome, totalExpense: s.totalExpense, totalSpend: s.totalSpend, revenue: s.revenue, margin: s.margin, incomePercent, expensePercent, goalTarget, goalHistory, lastUpdated: new Date().toISOString(), lastUpdatedBy: firebaseUid }); updateSync('realtime'); } catch(e) { updateSync('error'); } finally { isSyncing = false; } }, 500); };
+const saveLocal = () => { try { localStorage.setItem('fl_holders', JSON.stringify(holders)); localStorage.setItem('fl_ah', activeHolderId); localStorage.setItem('fl_aa', activeAccountId); localStorage.setItem('fl_cap', capital.toString()); localStorage.setItem('fl_reports', JSON.stringify(savedReports)); localStorage.setItem('fl_rps', reportPeriodStart || ''); localStorage.setItem('fl_ip', incomePercent.toString()); localStorage.setItem('fl_ep', expensePercent.toString()); localStorage.setItem('fl_goal', goalTarget.toString()); localStorage.setItem('fl_goal_history', JSON.stringify(goalHistory)); } catch(e) {} };
+const loadLocal = () => { try { const h = localStorage.getItem('fl_holders'); if (h) { holders = JSON.parse(h); activeHolderId = localStorage.getItem('fl_ah'); activeAccountId = localStorage.getItem('fl_aa'); if (!activeHolderId || !holders[activeHolderId]) activeHolderId = Object.keys(holders)[0]; const hh = holders[activeHolderId]; if (hh && (!activeAccountId || !hh.accounts[activeAccountId])) activeAccountId = Object.keys(hh.accounts || {})[0] || null; } else { const hid = 'h_' + Date.now(), aid = 'a_' + Date.now(); holders = { [hid]: { id: hid, name: 'Основной', accounts: { [aid]: { id: aid, name: 'Основной', balance: 0, transactions: [], createdAt: new Date().toISOString() } }, createdAt: new Date().toISOString() } }; activeHolderId = hid; activeAccountId = aid; } savedReports = JSON.parse(localStorage.getItem('fl_reports') || '{}'); reportPeriodStart = localStorage.getItem('fl_rps') || null; incomePercent = parseFloat(localStorage.getItem('fl_ip')) || 7; expensePercent = parseFloat(localStorage.getItem('fl_ep')) || 2.5; capital = parseFloat(localStorage.getItem('fl_cap')) || 0; goalTarget = parseFloat(localStorage.getItem('fl_goal')) || 0; goalHistory = JSON.parse(localStorage.getItem('fl_goal_history') || '[]'); } catch(e) {} };
+const saveServer = () => { if (!useFirebase || !isOnline) { updateSync('local'); return; } if (saveTimeout) clearTimeout(saveTimeout); saveTimeout = setTimeout(async () => { if (isSyncing) return; isSyncing = true; updateSync('syncing'); try { const s = calcAll(); const viewingOwner = getViewingOwner(); await firebase.database().ref(`users/${viewingOwner}`).update({ holders, activeHolderId, activeAccountId, capital, savedReports, reportPeriodStart, totalIncome: s.totalIncome, totalExpense: s.totalExpense, totalSpend: s.totalSpend, revenue: s.revenue, margin: s.margin, incomePercent, expensePercent, goalTarget, goalHistory, lastUpdated: new Date().toISOString(), lastUpdatedBy: firebaseUid }); updateSync('realtime'); } catch(e) { updateSync('error'); } finally { isSyncing = false; } }, 500); };
 const loadServer = () => { if (!useFirebase) { loadLocal(); renderAll(); updateSync('local'); return; } subscribeToData(); };
 const updateSync = (s) => { const el = document.getElementById('syncBadge'); const m = { syncing: { t: 'синхронизация...', i: 'cloud', c: 'syncing' }, synced: { t: 'сохранено', i: 'cloud', c: 'synced' }, realtime: { t: 'онлайн', i: 'dot', col: '#34C759', c: 'realtime' }, offline: { t: 'локально', i: 'disk', c: '' }, error: { t: 'ошибка', i: 'warn', c: '' }, local: { t: 'локально', i: 'disk', c: '' } }[s] || { t: '...', i: 'cloud', c: '' }; el.innerHTML = `${icon(m.i, 12, m.col)} ${m.t}`; el.className = 'sync-badge ' + m.c; };
 const getHolder = () => holders[activeHolderId];
 const getAccount = () => { const h = getHolder(); return h?.accounts?.[activeAccountId] || null; };
-const filterTx = (txs, p) => { if (!txs || p === 'all') return txs || []; const now = new Date(), today = new Date(now.getFullYear(), now.getMonth(), now.getDate()); if (p === 'custom') { if (!detailCustomRange) return txs; const cs = new Date(detailCustomRange.start + 'T00:00:00'), ce = new Date(detailCustomRange.end + 'T23:59:59.999'); return txs.filter(t => { const d = new Date(t.date); return d >= cs && d <= ce; }); } let s; if (p === 'today') s = today; else if (p === 'week') { s = new Date(today); s.setDate(s.getDate() - 6); } else if (p === 'month') { s = new Date(today); s.setMonth(s.getMonth() - 1); } else return txs; return txs.filter(t => new Date(t.date) >= s); };
+const filterTx = (txs, p) => { if (!txs) return []; if (p === undefined) { return reportPeriodStart ? txs.filter(t => new Date(t.date) >= new Date(reportPeriodStart)) : txs; } if (p === 'all') return txs; const now = new Date(), today = new Date(now.getFullYear(), now.getMonth(), now.getDate()); if (p === 'custom') { if (!detailCustomRange) return txs; const cs = new Date(detailCustomRange.start + 'T00:00:00'), ce = new Date(detailCustomRange.end + 'T23:59:59.999'); return txs.filter(t => { const d = new Date(t.date); return d >= cs && d <= ce; }); } let s; if (p === 'today') s = today; else if (p === 'week') { s = new Date(today); s.setDate(s.getDate() - 6); } else if (p === 'month') { s = new Date(today); s.setMonth(s.getMonth() - 1); } else return txs; return txs.filter(t => new Date(t.date) >= s); };
 function periodLabel(p) { if (p === 'today') return 'Сегодня'; if (p === 'week') return '7 дней'; if (p === 'month') return 'Месяц'; if (p === 'custom' && detailCustomRange) { const fd = (s) => { const d = new Date(s + 'T00:00:00'); return `${d.getDate().toString().padStart(2,'0')}.${(d.getMonth()+1).toString().padStart(2,'0')}.${d.getFullYear()}`; }; return `${fd(detailCustomRange.start)} – ${fd(detailCustomRange.end)}`; } return 'Всё время'; }
 const localDateStr = (d) => `${d.getFullYear()}-${(d.getMonth()+1).toString().padStart(2,'0')}-${d.getDate().toString().padStart(2,'0')}`;
 window.openCustomRangeSheet = () => { const todayStr = localDateStr(new Date()); document.getElementById('customRangeStart').value = detailCustomRange?.start || localDateStr(new Date(Date.now() - 29*86400000)); document.getElementById('customRangeEnd').value = detailCustomRange?.end || todayStr; document.getElementById('customRangeEnd').max = todayStr; document.getElementById('customRangeOverlay').classList.add('active'); document.getElementById('customRangeSheet').classList.add('active'); haptic('light'); };
 window.closeCustomRangeSheet = () => { document.getElementById('customRangeOverlay').classList.remove('active'); document.getElementById('customRangeSheet').classList.remove('active'); };
 window.applyCustomRange = () => { const start = document.getElementById('customRangeStart').value, end = document.getElementById('customRangeEnd').value; if (!start || !end) { showToast(icon('warn',20), 'Укажите обе даты'); return; } if (start > end) { showToast(icon('warn',20), 'Начало позже конца'); return; } detailCustomRange = { start, end }; closeCustomRangeSheet(); setPeriod('custom'); haptic('success'); };
 const calcAcc = (acc, period) => { let inc = 0, exp = 0, spend = 0; if (!acc?.transactions) return { totalIncome: 0, totalExpense: 0, totalSpend: 0, revenue: 0, margin: 0, turnover: 0 }; filterTx(acc.transactions, period).forEach(t => { if (!t || typeof t.amount !== 'number') return; if (t.type === 'income') inc += t.amount; else if (t.type === 'expense') exp += t.amount; else if (t.type === 'spend') spend += t.amount; }); const rev = inc * (incomePercent / 100) + exp * (expensePercent / 100); return { totalIncome: inc, totalExpense: exp, totalSpend: spend, revenue: rev, margin: rev - spend, turnover: inc + exp }; };
-const calcAll = () => { let inc = 0, exp = 0, spend = 0, bal = 0; Object.values(holders).forEach(h => { if (!h?.accounts) return; Object.values(h.accounts).forEach(a => { if (!a) return; const s = calcAcc(a); inc += s.totalIncome; exp += s.totalExpense; spend += s.totalSpend; bal += a.balance || 0; }); }); const rev = inc * (incomePercent / 100) + exp * (expensePercent / 100); return { totalIncome: inc, totalExpense: exp, totalSpend: spend, revenue: rev, margin: rev - spend, turnover: inc + exp, totalBalance: bal, siteBalance: capital - bal }; };
+const calcAll = (period) => { let inc = 0, exp = 0, spend = 0, bal = 0; Object.values(holders).forEach(h => { if (!h?.accounts) return; Object.values(h.accounts).forEach(a => { if (!a) return; const s = calcAcc(a, period); inc += s.totalIncome; exp += s.totalExpense; spend += s.totalSpend; bal += a.balance || 0; }); }); const rev = inc * (incomePercent / 100) + exp * (expensePercent / 100); return { totalIncome: inc, totalExpense: exp, totalSpend: spend, revenue: rev, margin: rev - spend, turnover: inc + exp, totalBalance: bal, siteBalance: capital - bal }; };
 function loadTeamMembers() {
 if (!useFirebase) return;
 const viewingOwner = getViewingOwner();
 firebase.database().ref(`users/${viewingOwner}/team`).on('value', snap => {
 teamMembers = snap.val() || {};
 if (viewingOwner === firebaseUid) currentUserRole = 'owner';
 else if (teamMembers[firebaseUid]) currentUserRole = teamMembers[firebaseUid].role || 'viewer';
 else currentUserRole = 'viewer';
 renderTeamMembers(); updateUIByRole();
 });
@@ -1520,25 +1530,28 @@ item.innerHTML = `<div class="tx-info"><div class="tx-desc">${desc}</div><div cl
 w.appendChild(sa); w.appendChild(item); list.appendChild(w);
 initSwipeGesture(w, item);
 });
 document.getElementById('resetBtn').style.display = canDelete() ? 'block' : 'none';
 }
 saveLocal();
 updateMiniRing();
 };
 window.resetAll = () => { if (!canDelete()) return; const acc = getAccount(), h = getHolder(); if (!acc || !h || !acc.transactions?.length) { showToast(icon('warn',20),'Нет операций'); return; } haptic('warning'); const r = () => { acc.balance = 0; acc.transactions = []; renderAll(); saveLocal(); saveServer(); haptic('success'); showToast(icon('check',20),'Обнулено'); }; safeConfirm(`Обнулить "${acc.name}"?`, r); };
 window.deleteTransaction = (idx) => { if (!canEdit()) return; const acc = getAccount(); if (!acc?.transactions) return; const ri = acc.transactions.length - 1 - idx; if (ri < 0 || ri >= acc.transactions.length) return; const tx = acc.transactions[ri]; const del = () => { if (tx.type === 'income') { acc.balance -= tx.amount; capital -= tx.amount * (incomePercent / 100); } else if (tx.type === 'expense') { acc.balance += tx.amount; capital -= tx.amount * (expensePercent / 100); } else if (tx.type === 'adjustment') { acc.balance -= tx.amount; } else if (tx.type === 'revenue') { capital -= tx.amount; } else if (tx.type === 'capital_adj') { capital -= tx.amount; } acc.transactions.splice(ri, 1); renderAll(); saveServer(); haptic('success'); showToast(icon('trash',20),'Удалено'); }; safeConfirm('Удалить?', del); };
-const refreshReport = () => { const s = calcAll(); ['sumTurnover', 'sumRevenue', 'sumIncome', 'sumExpense', 'sumSpend', 'sumBalance', 'sumCapital', 'sumSite'].forEach((id, i) => animateValue(document.getElementById(id), [s.turnover, s.revenue, s.totalIncome, s.totalExpense, s.totalSpend, s.totalBalance, capital, s.siteBalance][i], fmt)); const me = document.getElementById('sumMargin'); animateValue(me, s.margin, fmt); s.margin < 0 ? me.classList.add('negative') : me.classList.remove('negative'); renderReportsList(); checkLastFixed(); };
+const ruDays = (n) => { const mod10 = n % 10, mod100 = n % 100; if (mod10 === 1 && mod100 !== 11) return 'день'; if ([2,3,4].includes(mod10) && ![12,13,14].includes(mod100)) return 'дня'; return 'дней'; };
+const reportPeriodLabel = () => { if (!reportPeriodStart) return 'Текущий период: за всё время'; const days = Math.max(1, Math.ceil((Date.now() - new Date(reportPeriodStart).getTime()) / 86400000)); return `Текущий период: с ${fmtDateShort(reportPeriodStart)} · ${days} ${ruDays(days)}`; };
+const refreshReport = () => { const s = calcAll(); ['sumTurnover', 'sumRevenue', 'sumIncome', 'sumExpense', 'sumSpend', 'sumBalance', 'sumCapital', 'sumSite'].forEach((id, i) => animateValue(document.getElementById(id), [s.turnover, s.revenue, s.totalIncome, s.totalExpense, s.totalSpend, s.totalBalance, capital, s.siteBalance][i], fmt)); const me = document.getElementById('sumMargin'); animateValue(me, s.margin, fmt); s.margin < 0 ? me.classList.add('negative') : me.classList.remove('negative'); document.getElementById('currentPeriodLabel').textContent = reportPeriodLabel(); renderReportsList(); checkLastFixed(); };
 const renderReport = refreshReport;
-const renderReportsList = () => { const list = document.getElementById('reportsList'), keys = Object.keys(savedReports).sort().reverse(); if (!keys.length) { list.innerHTML = '<div class="empty-state"><div class="empty-text">Нет отчётов</div></div>'; return; } list.innerHTML = ''; keys.forEach(key => { const r = savedReports[key]; if (!r) return; const d = new Date(r.date), sp = r.spend || 0, mg = r.margin ?? ((r.revenue || 0) - sp); const c = document.createElement('div'); c.className = 'report-card'; c.innerHTML = `<div class="report-header"><div><div class="report-date">${d.getDate()} ${MONTHS[d.getMonth()]} ${d.getFullYear()}</div><div class="report-time">${d.toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' })}</div></div><div class="report-actions"><button class="report-action share" onclick="shareReport('${key}')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg></button>${canDelete() ? `<button class="report-action delete" onclick="deleteReport('${key}')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg></button>` : ''}</div></div><div class="report-stats"><div class="report-stat"><span class="report-stat-label">${icon('chartUp',15,'#007AFF')}</span><span class="report-stat-value text-blue">${fmt(r.turnover)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('money',15,'#FF9500')}</span><span class="report-stat-value text-orange">${fmt(r.revenue)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('dot',15,'#34C759')}</span><span class="report-stat-value text-green">${fmt(r.income)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('dot',15,'#FF3B30')}</span><span class="report-stat-value text-red">${fmt(r.expense)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('bag',15,'#AF52DE')}</span><span class="report-stat-value text-purple">${fmt(sp)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('trendUp2',15,'#5AC8FA')}</span><span class="report-stat-value text-teal">${fmt(mg)}</span></div></div>`; list.appendChild(c); }); };
-const checkLastFixed = () => { const keys = Object.keys(savedReports).sort().reverse(), b = document.getElementById('lastFixedBadge'); if (keys.length) { const d = new Date(savedReports[keys[0]].date); document.getElementById('lastFixedDate').textContent = `${d.getDate()} ${MONTHS[d.getMonth()]} ${d.getFullYear()}`; b.style.display = 'flex'; } else b.style.display = 'none'; };
-const refreshDetail = () => { const s = calcAll(); ['detBalance', 'detCapital', 'detSite', 'detSpend', 'detRevenue'].forEach((id, i) => animateValue(document.getElementById(id), [s.totalBalance, capital, s.siteBalance, s.totalSpend, s.revenue][i], fmt)); const me = document.getElementById('detMargin'); animateValue(me, s.margin, fmt); s.margin < 0 ? me.classList.add('negative') : me.classList.remove('negative'); renderDetailTree(); renderCompare(); renderLeaderboard(); };
+const fmtDateShort = (iso) => { const d = new Date(iso); return `${d.getDate()} ${MONTHS[d.getMonth()]} ${d.getFullYear()}`; };
+const renderReportsList = () => { const list = document.getElementById('reportsList'), keys = Object.keys(savedReports).sort().reverse(); if (!keys.length) { list.innerHTML = '<div class="empty-state"><div class="empty-text">Нет отчётов</div></div>'; return; } list.innerHTML = ''; keys.forEach(key => { const r = savedReports[key]; if (!r) return; const d = new Date(r.date), sp = r.spend || 0, mg = r.margin ?? ((r.revenue || 0) - sp); const isSubmit = r.type === 'submit'; const dateLabel = isSubmit ? `${r.periodStart ? fmtDateShort(r.periodStart) : 'С начала'} – ${fmtDateShort(r.date)}` : fmtDateShort(r.date); const badge = isSubmit ? `<span style="display:inline-flex;font-size:0.65rem;font-weight:600;color:var(--ios-green);background:rgba(52,199,89,0.12);padding:2px 8px;border-radius:100px;margin-top:2px;">Сдан</span>` : `<span style="display:inline-flex;font-size:0.65rem;font-weight:600;color:var(--ios-text-secondary);background:rgba(120,120,128,0.12);padding:2px 8px;border-radius:100px;margin-top:2px;">Фиксация</span>`; const c = document.createElement('div'); c.className = 'report-card'; c.innerHTML = `<div class="report-header"><div><div class="report-date">${dateLabel}</div><div class="report-time">${d.toLocaleTimeString('ru-RU', { hour: '2-digit', minute: '2-digit' })}</div>${badge}</div><div class="report-actions"><button class="report-action share" onclick="shareReport('${key}')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg></button>${canDelete() ? `<button class="report-action delete" onclick="deleteReport('${key}')"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg></button>` : ''}</div></div><div class="report-stats"><div class="report-stat"><span class="report-stat-label">${icon('chartUp',15,'#007AFF')}</span><span class="report-stat-value text-blue">${fmt(r.turnover)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('money',15,'#FF9500')}</span><span class="report-stat-value text-orange">${fmt(r.revenue)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('dot',15,'#34C759')}</span><span class="report-stat-value text-green">${fmt(r.income)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('dot',15,'#FF3B30')}</span><span class="report-stat-value text-red">${fmt(r.expense)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('bag',15,'#AF52DE')}</span><span class="report-stat-value text-purple">${fmt(sp)}</span></div><div class="report-stat"><span class="report-stat-label">${icon('trendUp2',15,'#5AC8FA')}</span><span class="report-stat-value text-teal">${fmt(mg)}</span></div></div>`; list.appendChild(c); }); };
+const checkLastFixed = () => { const keys = Object.keys(savedReports).sort().reverse(), b = document.getElementById('lastFixedBadge'), undoBtn = document.getElementById('lastFixedUndo'); if (keys.length) { const last = savedReports[keys[0]], d = new Date(last.date); document.getElementById('lastFixedDate').textContent = `${d.getDate()} ${MONTHS[d.getMonth()]} ${d.getFullYear()}`; b.style.display = 'flex'; undoBtn.style.display = last.type === 'submit' ? 'inline' : 'none'; } else { b.style.display = 'none'; } };
+const refreshDetail = () => { const s = calcAll(detailPeriod); ['detBalance', 'detCapital', 'detSite', 'detSpend', 'detRevenue'].forEach((id, i) => animateValue(document.getElementById(id), [s.totalBalance, capital, s.siteBalance, s.totalSpend, s.revenue][i], fmt)); const me = document.getElementById('detMargin'); animateValue(me, s.margin, fmt); s.margin < 0 ? me.classList.add('negative') : me.classList.remove('negative'); renderDetailTree(); renderCompare(); renderLeaderboard(); };
 function computeHolderRanking() {
 return Object.keys(holders).map(hid => {
 const h = holders[hid];
 const accounts = Object.values(h.accounts || {}).filter(Boolean);
 let revenue = 0;
 accounts.forEach(a => { revenue += calcAcc(a, detailPeriod).revenue; });
 return { hid, name: h.name, revenue };
 }).filter(r => r.revenue !== 0).sort((a, b) => b.revenue - a.revenue);
 }
 function renderLeaderboard() {
@@ -1699,31 +1712,36 @@ c.appendChild(holderCard);
 });
 };
 const refreshSettings = () => { document.getElementById('setIncPct').value = incomePercent; document.getElementById('setExpPct').value = expensePercent; document.getElementById('curIncPct').textContent = incomePercent; document.getElementById('curExpPct').textContent = expensePercent; renderTeamMembers(); };
 const doTransaction = (type) => { if (!canEdit()) return; const acc = getAccount(); if (!acc) { showToast(icon('warn',20),'Выберите счёт'); return; } const amount = parseFloat(document.getElementById('amountInput').value); if (!amount || amount <= 0) { showToast(icon('warn',20),'Введите сумму'); haptic('error'); return; } if (amount > 999999999) return; if (type === 'income') acc.balance += amount; else acc.balance -= amount; if (!acc.transactions) acc.transactions = []; acc.transactions.push({ type, amount, desc: type === 'income' ? 'Пополнение' : 'Списание', date: new Date().toISOString() }); let rev = 0; if (type === 'income') rev = amount * (incomePercent / 100); else if (type === 'expense') rev = amount * (expensePercent / 100); if (rev > 0) { capital += rev; acc.transactions.push({ type: 'revenue', amount: rev, desc: 'Капитализация', date: new Date().toISOString() }); } document.getElementById('amountInput').value = ''; haptic('success'); renderAll(); saveServer(); if (rev > 0) setTimeout(() => showToast(icon('money',20),`+${fmt(rev)} к капиталу`), 300); };
 window.openQuickExpense = () => { if (!canEdit()) return; if (!getAccount()) { showToast(icon('warn',20),'Выберите счёт'); return; } document.getElementById('quickExpenseAmount').value = ''; document.getElementById('quickExpenseNote').value = ''; document.getElementById('quickExpenseOverlay').classList.add('active'); document.getElementById('quickExpenseSheet').classList.add('active'); haptic('light'); setTimeout(() => document.getElementById('quickExpenseAmount').focus(), 400); };
 window.closeQuickExpense = () => { document.getElementById('quickExpenseOverlay').classList.remove('active'); document.getElementById('quickExpenseSheet').classList.remove('active'); };
 window.submitQuickExpense = () => { if (!canEdit()) return; const acc = getAccount(); if (!acc) return; const a = parseFloat(document.getElementById('quickExpenseAmount').value), n = document.getElementById('quickExpenseNote').value.trim(); if (!a || a <= 0) { showToast(icon('warn',20),'Введите сумму'); haptic('error'); return; } acc.balance -= a; if (!acc.transactions) acc.transactions = []; acc.transactions.push({ type: 'spend', amount: a, desc: n || 'Трата', date: new Date().toISOString() }); haptic('success'); renderAll(); saveLocal(); saveServer(); closeQuickExpense(); showToast(icon('check',20),n ? `"${n}" учтена` : 'Трата учтена'); };
 window.openEditBalance = () => { if (!canEdit()) return; const acc = getAccount(); if (!acc) { showToast(icon('warn',20),'Выберите счёт'); return; } document.getElementById('editBalanceInput').value = ''; document.getElementById('editBalanceComment').value = ''; document.getElementById('editBalanceCurrent').textContent = fmt(acc.balance); document.getElementById('editBalanceOverlay').classList.add('active'); document.getElementById('editBalanceSheet').classList.add('active'); haptic('light'); setTimeout(() => document.getElementById('editBalanceInput').focus(), 400); };
 window.closeEditBalance = () => { document.getElementById('editBalanceOverlay').classList.remove('active'); document.getElementById('editBalanceSheet').classList.remove('active'); };
 window.saveEditBalance = () => { if (!canEdit()) return; const acc = getAccount(); if (!acc) return; const nb = parseFloat(document.getElementById('editBalanceInput').value); if (isNaN(nb)) { showToast(icon('warn',20),'Введите сумму'); haptic('error'); return; } if (nb === acc.balance) { showToast(icon('info',20),'Не изменился'); closeEditBalance(); return; } const c = document.getElementById('editBalanceComment').value.trim(); if (!acc.transactions) acc.transactions = []; acc.transactions.push({ type: 'adjustment', amount: nb - acc.balance, desc: c || 'Корректировка', date: new Date().toISOString() }); acc.balance = nb; haptic('success'); renderAll(); saveServer(); closeEditBalance(); showToast(icon('check',20),`Баланс: ${fmt(nb)}`); };
-const doFixReport = () => { if (!canEdit()) return; const s = calcAll(), now = new Date(), key = now.toISOString().replace(/[:.]/g, '-').slice(0, 19); savedReports[key] = { date: now.toISOString(), turnover: s.turnover, income: s.totalIncome, expense: s.totalExpense, spend: s.totalSpend, revenue: s.revenue, margin: s.margin, balance: s.totalBalance, capital, siteBalance: s.siteBalance }; saveLocal(); saveServer(); renderReportsList(); checkLastFixed(); haptic('success'); showToast(icon('check',20),'Отчёт сохранён'); };
+window.openReportChoice = () => { if (!canEdit()) return; document.getElementById('reportChoiceOverlay').classList.add('active'); document.getElementById('reportChoiceSheet').classList.add('active'); haptic('light'); };
+window.closeReportChoice = () => { document.getElementById('reportChoiceOverlay').classList.remove('active'); document.getElementById('reportChoiceSheet').classList.remove('active'); };
+const saveReportSnapshot = (extra) => { const s = calcAll(), now = new Date(), key = now.toISOString().replace(/[:.]/g, '-').slice(0, 19); savedReports[key] = Object.assign({ date: now.toISOString(), turnover: s.turnover, income: s.totalIncome, expense: s.totalExpense, spend: s.totalSpend, revenue: s.revenue, margin: s.margin, balance: s.totalBalance, capital, siteBalance: s.siteBalance }, extra); return key; };
+window.doJustFixReport = () => { if (!canEdit()) return; saveReportSnapshot({ type: 'fix' }); saveLocal(); saveServer(); closeReportChoice(); renderAll(); haptic('success'); showToast(icon('check',20),'Отчёт сохранён'); };
+window.doSubmitReport = () => { if (!canEdit()) return; const periodStart = reportPeriodStart; saveReportSnapshot({ type: 'submit', periodStart }); reportPeriodStart = new Date().toISOString(); saveLocal(); saveServer(); closeReportChoice(); renderAll(); haptic('success'); showToast(icon('check',20),'Отчёт сдан, период сброшен'); };
+window.undoLastSubmit = () => { if (!canEdit()) return; const keys = Object.keys(savedReports).sort().reverse(); if (!keys.length) return; const key = keys[0], entry = savedReports[key]; if (entry.type !== 'submit') return; const undo = () => { reportPeriodStart = entry.periodStart; delete savedReports[key]; saveLocal(); saveServer(); renderAll(); haptic('success'); showToast(icon('check',20),'Сдача отчёта отменена'); }; safeConfirm('Отменить последнюю сдачу отчёта? Показатели вернутся к состоянию до сдачи.', undo); };
 const shareText = async (text, title) => { haptic('light'); if (tg?.switchInlineQuery) { try { tg.switchInlineQuery(text, ['users', 'groups', 'channels']); return; } catch (e) { } } if (navigator.share) { try { await navigator.share({ title, text }); return; } catch (e) { if (e.name === 'AbortError') return; } } try { await navigator.clipboard.writeText(text); showToast(icon('copy',20),'Скопировано'); } catch (e) { prompt('Скопируйте:', text); } };
 const doShareCurrentReport = () => { const s = calcAll(), now = new Date(); shareText(`📊 Финансовый отчёт Findr\n📅 ${now.getDate()} ${MONTHS[now.getMonth()]} ${now.getFullYear()}\n💹 Оборот: ${fmt(s.turnover)}\n💰 Выручка: ${fmt(s.revenue)}\n📈 Маржа: ${fmt(s.margin)}\n💜 Капитал: ${fmt(capital)}`, 'Отчёт Findr'); };
-window.shareReport = (key) => { const r = savedReports[key]; if (!r) return; const d = new Date(r.date); shareText(`📊 Отчёт Findr\n📅 ${d.getDate()} ${MONTHS[d.getMonth()]} ${d.getFullYear()}\n💹 Оборот: ${fmt(r.turnover)}\n💰 Выручка: ${fmt(r.revenue)}\n📈 Маржа: ${fmt(r.margin ?? (r.revenue - (r.spend || 0)))}`, 'Отчёт Findr'); };
+window.shareReport = (key) => { const r = savedReports[key]; if (!r) return; const dateLine = r.type === 'submit' ? `${r.periodStart ? fmtDateShort(r.periodStart) : 'с начала'} – ${fmtDateShort(r.date)}` : fmtDateShort(r.date); shareText(`📊 Отчёт Findr\n📅 ${dateLine}\n💹 Оборот: ${fmt(r.turnover)}\n💰 Выручка: ${fmt(r.revenue)}\n📈 Маржа: ${fmt(r.margin ?? (r.revenue - (r.spend || 0)))}`, 'Отчёт Findr'); };
 window.shareDetail = () => { shareText(`📊 Детализация Findr\n💰 Капитал: ${fmt(capital)}\n💹 Выручка: ${fmt(calcAll().revenue)}`, 'Детализация Findr'); };
 const accountReportLines = (a, s) => `💼 Баланс: ${fmt(a.balance)}\n💹 Оборот: ${fmt(s.turnover)}\n🟢 Доход: ${fmt(s.totalIncome)}\n🔴 Расход: ${fmt(s.totalExpense)}\n💰 Выручка: ${fmt(s.revenue)}\n🛍️ Траты: ${fmt(s.totalSpend)}\n📈 Маржа: ${fmt(s.margin)}`;
 window.shareAccountDetail = (hid, aid) => { const h = holders[hid]; if (!h) return; const a = h.accounts?.[aid]; if (!a) return; const s = calcAcc(a, detailPeriod); const text = `📊 Отчёт по счёту «${a.name}»\n👤 Держатель: ${h.name}\n🗓️ Период: ${periodLabel(detailPeriod)}\n\n${accountReportLines(a, s)}`; shareText(text, `Отчёт: ${a.name}`); };
 window.shareHolderDetail = (hid) => { const h = holders[hid]; if (!h) return; const accounts = Object.values(h.accounts || {}).filter(Boolean); if (!accounts.length) { showToast(icon('warn',20), 'Нет счетов'); return; } let totalBalance = 0; const parts = accounts.map(a => { const s = calcAcc(a, detailPeriod); totalBalance += a.balance || 0; return `— ${a.name} —\n${accountReportLines(a, s)}`; }); const text = `📊 Детализированный отчёт по держателю «${h.name}»\n🗓️ Период: ${periodLabel(detailPeriod)}\n\n${parts.join('\n\n')}\n\n📦 Итого по держателю: ${fmt(totalBalance)}`; shareText(text, `Отчёт: ${h.name}`); };
 window.deleteReport = (key) => { if (!canDelete()) return; const del = () => { delete savedReports[key]; saveLocal(); saveServer(); renderReportsList(); checkLastFixed(); haptic('success'); showToast(icon('trash',20),'Удалено'); }; safeConfirm('Удалить?', del); };
 const doSaveSettings = () => { if (!canChangeSettings()) return; const ni = parseFloat(document.getElementById('setIncPct').value); const ne = parseFloat(document.getElementById('setExpPct').value); if (isNaN(ni) || isNaN(ne)) { showToast(icon('warn',20),'Ошибка'); return; } incomePercent = ni; expensePercent = ne; document.getElementById('curIncPct').textContent = incomePercent; document.getElementById('curExpPct').textContent = expensePercent; renderAll(); saveServer(); haptic('success'); showToast(icon('check',20),'Сохранено'); };
-window.setPeriod = (p) => { detailPeriod = p; document.querySelectorAll('.period-chip').forEach(c => c.classList.toggle('active', c.dataset.p === p)); const cc = document.getElementById('customPeriodChip'); if (cc) cc.innerHTML = (p === 'custom' && detailCustomRange) ? `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;margin-right:3px;"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>${periodLabel('custom')}` : `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;margin-right:3px;"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>Свой период`; renderDetailTree(); renderCompare(); renderLeaderboard(); haptic('light'); };
+window.setPeriod = (p) => { detailPeriod = p; document.querySelectorAll('.period-chip').forEach(c => c.classList.toggle('active', c.dataset.p === p)); const cc = document.getElementById('customPeriodChip'); if (cc) cc.innerHTML = (p === 'custom' && detailCustomRange) ? `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;margin-right:3px;"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>${periodLabel('custom')}` : `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="vertical-align:-2px;margin-right:3px;"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>Свой период`; refreshDetail(); renderDetailTree(); renderCompare(); renderLeaderboard(); haptic('light'); };
 
 // ===== УЛУЧШЕННЫЙ ACCOUNT SHEET =====
 window.openAccountSheet = () => {
 document.getElementById('accountSearch').value = '';
 isEditMode = false;
 updateEditModeButton();
 renderAccountTree();
 document.getElementById('accountOverlay').classList.add('active');
 document.getElementById('accountSheet').classList.add('active');
 haptic('light');
diff --git a/test/make-test-copy.sh b/test/make-test-copy.sh
new file mode 100644
index 0000000..7c7362d
--- /dev/null
+++ b/test/make-test-copy.sh
@@ -0,0 +1,12 @@
+#!/usr/bin/env bash
+# Regenerates index.test.html from index.html with a deliberately invalid
+# Firebase config, so browser-driven verification never reaches the real
+# production database (financesimple-7c73a). Re-run after every edit to
+# index.html, before any Playwright verification step.
+set -e
+cd "$(dirname "$0")/.."
+cp index.html index.test.html
+sed -i 's/apiKey: "AIzaSyAbeGi5WVmiszMO9muzZV4CZQxXe17T7UY"/apiKey: "TEST-INVALID-KEY"/' index.test.html
+sed -i 's#databaseURL: "https://financesimple-7c73a-default-rtdb.europe-west1.firebasedatabase.app"#databaseURL: "https://test-invalid.invalid"#' index.test.html
+sed -i 's/financesimple-7c73a/test-invalid-project/g' index.test.html
+grep -q "TEST-INVALID-KEY" index.test.html && ! grep -q "financesimple-7c73a" index.test.html && echo "OK: test copy uses dummy Firebase config"
